# ProfitableWeb Frontend - Правила разработки

## 📦 Пакетный менеджер
- **Используем Bun** для всех операций с пакетами
- Команды: `bun install`, `bun add`, `bun update`, `bun run`
- НЕ используем npm/yarn - только Bun
- Bun.lock файл в репозитории

## 🎨 Стилизация
- **НЕ используем module.scss** - только обычные .scss файлы
- Используем CSS-переменные из `src/styles/utils/_variables.scss`
- Применяем миксины из `src/styles/utils/_mixins.scss`
- Следуем breakpoints из `src/styles/utils/_breakpoints.scss`
- Поддерживаем темную/светлую тему через CSS-переменные

## 🏗️ Архитектура компонентов
- Компоненты в `src/components/` с четкой иерархией папок
- Каждый компонент в своей папке с файлами:
  - `ComponentName.tsx` - основной компонент
  - `ComponentName.scss` - стили (НЕ module.scss!)
  - `index.ts` - экспорт компонента
- Используем TypeScript интерфейсы для пропсов
- Экспортируем компоненты через index.ts файлы

## 📁 Структура файлов
```
src/components/
  component-name/
    ComponentName.tsx
    ComponentName.scss
    index.ts
```

## 🎯 Именование
- Компоненты: PascalCase (AppBar, UserProfile)
- Файлы компонентов: PascalCase (AppBar.tsx)
- SCSS файлы: PascalCase (AppBar.scss)
- CSS классы: kebab-case (.app-bar, .user-profile)
- Переменные SCSS: kebab-case с префиксами

## 🔧 TypeScript
- Строгая типизация для всех пропсов
- Используем интерфейсы вместо type для пропсов
- Экспортируем типы через index.ts в папке types
- Избегаем any, используем unknown при необходимости

## 🎨 SCSS Правила
- Импортируем переменные и миксины в начале файла
- Используем вложенность не глубже 3 уровней
- Применяем BEM-методологию для именования классов
- Группируем стили: layout → typography → colors → effects

## 📱 Адаптивность
- Mobile-first подход
- Используем breakpoints из _breakpoints.scss
- Тестируем на всех размерах экранов

## ♿ Доступность
- Всегда добавляем aria-label для интерактивных элементов
- Используем семантические HTML теги
- Обеспечиваем навигацию с клавиатуры
- Контрастность цветов соответствует WCAG

## 🚀 Производительность
- Ленивая загрузка для тяжелых компонентов
- Мемоизация через React.memo при необходимости
- Оптимизация изображений
- Минимизация bundle size

## 🧪 Тестирование
- Тесты рядом с компонентами в папке __tests__
- Используем Vitest для unit тестов
- Покрытие тестами критических компонентов

## 📦 Импорты
- Абсолютные импорты через алиасы (если настроены)
- Группировка импортов: React → библиотеки → внутренние модули
- Именованные экспорты предпочтительнее default

## 🎭 Темы
- Используем CSS-переменные для цветов
- Поддержка темной/светлой темы через data-атрибуты
- Плавные переходы между темами

## 🔍 Code Review
- Проверяем соответствие правилам именования
- Убеждаемся в отсутствии module.scss
- Проверяем доступность и семантику
- Тестируем на разных устройствах

---
*Эти правила живут и эволюционируют вместе с проектом* ✨
